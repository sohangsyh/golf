#!/bin/bash
# Double-click this file in Finder to start SwingVision.
# (First time only: macOS may show a security warning since this file
# wasn't downloaded from the App Store — right-click it and choose
# "Open" instead of double-clicking to bypass that just once.)
cd "$(dirname "$0")"
npm run go
